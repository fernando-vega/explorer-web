import { FinalTextPipe } from './final-text.pipe';

describe('FinalTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FinalTextPipe();
    expect(pipe).toBeTruthy();
  });
});
